# @package _global_

# CIFAR-100 — fixed-lambda AdaBreg pruning of ResNet-18.
# Inherits cifar10_bregman_adabreg_fixed; swaps only the dataset.
# Run: python src/train.py experiment=img/cifar100_bregman_adabreg_fixed

defaults:
  - /experiment/img/cifar10_bregman_adabreg_fixed
  - override /datamodule: datasets/cifar100.yaml
  - _self_

tags: ["img", "cifar100", "Bregman_learning", "Adabreg", "fixed"]
