# @package _global_

# CIFAR-100 — adaptive AdaBreg pruning of ResNet-18.
# Inherits cifar10_bregman_adabreg; swaps only the dataset.
# Run: python src/train.py experiment=img/cifar100_bregman_adabreg

defaults:
  - /experiment/img/cifar10_bregman_adabreg
  - override /datamodule: datasets/cifar100.yaml
  - _self_

tags: ["img", "cifar100", "Bregman_learning", "Adabreg"]
