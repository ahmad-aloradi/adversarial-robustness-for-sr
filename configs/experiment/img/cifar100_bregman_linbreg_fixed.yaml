# @package _global_

# CIFAR-100 — fixed-lambda LinBreg pruning of ResNet-18.
# Inherits cifar10_bregman_linbreg_fixed; swaps only the dataset.
# Run: python src/train.py experiment=img/cifar100_bregman_linbreg_fixed

defaults:
  - /experiment/img/cifar10_bregman_linbreg_fixed
  - override /datamodule: datasets/cifar100.yaml
  - _self_

tags: ["img", "cifar100", "Bregman_learning", "Linbreg", "fixed"]
