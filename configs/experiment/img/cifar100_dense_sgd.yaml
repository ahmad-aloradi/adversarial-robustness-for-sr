# @package _global_

# ResNet-18 dense SGD baseline on CIFAR-100 (~77-78% top-1).
# Run: python src/train.py experiment=img/cifar100_dense_sgd

defaults:
  - /experiment/img/cifar10_dense_sgd
  - override /datamodule: datasets/cifar100.yaml
  - _self_

tags: ["img", "cifar100", "dense", "sgd"]
