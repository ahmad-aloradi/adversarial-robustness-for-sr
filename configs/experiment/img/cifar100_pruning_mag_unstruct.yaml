# @package _global_

# CIFAR-100 — global unstructured gradual magnitude pruning of ResNet-18.
# Inherits cifar10_pruning_mag_unstruct; swaps only the dataset.
# Run: python src/train.py experiment=img/cifar100_pruning_mag_unstruct

defaults:
  - /experiment/img/cifar10_pruning_mag_unstruct
  - override /datamodule: datasets/cifar100.yaml
  - _self_

tags: ["img", "cifar100", "pruning", "l1_unstructured", "gradual"]
