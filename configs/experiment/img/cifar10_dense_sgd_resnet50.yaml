# @package _global_

# ResNet-50 dense SGD baseline on CIFAR-10 (same recipe, deeper backbone).
# Inherits cifar10_dense_sgd; swaps only the backbone.
# Run: python src/train.py experiment=img/cifar10_dense_sgd_resnet50

defaults:
  - /experiment/img/cifar10_dense_sgd
  - override /module/img_model: resnet50
  - _self_

tags: ["img", "cifar10", "dense", "sgd", "resnet50"]
