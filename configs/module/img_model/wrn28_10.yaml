# @package module.model.net
# CIFAR-only backbone (32x32 fixed pool) — see src/modules/models/wide_resnet.py.
_target_: src.modules.models.wide_resnet.build_wide_resnet
depth: 28
widen_factor: 10
num_classes: ${datamodule.num_classes}
in_channels: 3 # fixed input contract; every dataset emits 3-channel images
