# @package module.model.net
_target_: src.modules.models.vision_resnet.build_resnet
arch: wide_resnet101_2
num_classes: ${datamodule.num_classes}
in_channels: 3 # fixed input contract; every dataset emits 3-channel images
dataset_name: ${datamodule.name} # picks the stem via _IMAGE_SIZES
