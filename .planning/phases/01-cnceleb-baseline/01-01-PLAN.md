---
phase: 01-cnceleb-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - configs/experiment/sv/sv_wespeaker.yaml
autonomous: false

must_haves:
  truths:
    - "Training config uses 50 epochs (not 15)"
    - "LR schedule reaches ~0.0001 final LR (not 0.0044)"
    - "Margin scheduling starts at ~13% of training (epoch 7)"
    - "Warmup starts from 0.0 (not 0.01)"
    - "Multiple checkpoints saved for post-training averaging"
  artifacts:
    - path: "configs/experiment/sv/sv_wespeaker.yaml"
      provides: "Fixed WeSpeaker training recipe"
      contains: "max_epochs: 50"
  key_links:
    - from: "configs/experiment/sv/sv_wespeaker.yaml"
      to: "src/utils/schedulers.WarmupExponentialLR"
      via: "lr_scheduler.scheduler._target_"
      pattern: "WarmupExponentialLR"
    - from: "configs/experiment/sv/sv_wespeaker.yaml"
      to: "src/callbacks/margin_scheduler.ProgressiveMarginScheduler"
      via: "callbacks.margin_scheduler._target_"
      pattern: "ProgressiveMarginScheduler"
---

<objective>
Fix CNCeleb training hyperparameters to match WeSpeaker reference recipe.

Purpose: Current config uses 15 epochs with aggressive LR decay, resulting in ~10.8% EER vs WeSpeaker's 7.8%. The research identified 4 issues: training too short, final LR too high, margin timing wrong, warmup starting non-zero.

Output: Updated `sv_wespeaker.yaml` with proportionally scaled hyperparameters for 50-epoch training, configured to save multiple checkpoints for averaging.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-cnceleb-baseline/01-RESEARCH.md
@.planning/phases/01-cnceleb-baseline/01-RESEARCH-ADDENDUM.md

# Key source files
@configs/experiment/sv/sv_wespeaker.yaml
@configs/callbacks/model_checkpoint.yaml
@src/utils/schedulers.py
@src/callbacks/margin_scheduler.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update sv_wespeaker.yaml with correct hyperparameters</name>
  <files>configs/experiment/sv/sv_wespeaker.yaml</files>
  <action>
Update the experiment config with proportionally scaled hyperparameters from WeSpeaker reference:

1. **trainer section:**
   - `max_epochs`: 15 -> 50

2. **callbacks.early_stopping:**
   - `patience`: 8 -> 15 (proportional increase)

3. **callbacks.model_checkpoint:**
   - Add override: `save_top_k: 10` (save last 10 checkpoints for averaging)
   - This overrides the default save_top_k: 1 from model_checkpoint.yaml
   - WeSpeaker averages 10 checkpoints for final model

4. **callbacks.margin_scheduler:**
   - `warmup_epochs`: 2 -> 13 (WeSpeaker: 20 epochs out of 150 -> scaled for 50 epochs)
   - `start_epoch`: 2 -> 7 (WeSpeaker: starts at 20/150 = 13% -> 50 * 0.13 = 6.5, round to 7)
   - Update comment to reflect new scaling

5. **module.lr_scheduler.scheduler:**
   - `warmup_epochs`: 1 -> 3 (WeSpeaker: 6/150 = 4% -> 50 * 0.04 = 2, rounded up to 3)
   - `warmup_start_factor`: 0.01 -> 0.0 (match WeSpeaker's warm_from_zero: True)
   - `gamma`: 0.80 -> 0.913 (calculated to reach ~0.0001 final LR after 47 post-warmup epochs)
   - Update comment to explain gamma calculation

6. **Update header comment:**
   - Change "~15 epoch training" references to "~50 epoch training"
   - Add note about gamma calculation: 0.1 * 0.913^47 ~ 0.00015
   - Add note about checkpoint averaging workflow

DO NOT change: optimizer settings (SGD with lr=0.1), batch sizes, augmentation config, loss function settings (margin=0.0 initial, scale=32.0).
  </action>
  <verify>
Verify config syntax is valid:
```bash
python -c "from omegaconf import OmegaConf; OmegaConf.load('configs/experiment/sv/sv_wespeaker.yaml')"
```

Check key values are updated:
```bash
grep -E "(max_epochs|warmup_epochs|warmup_start_factor|gamma|start_epoch|patience|save_top_k)" configs/experiment/sv/sv_wespeaker.yaml
```

Expected output should show:
- max_epochs: 50
- warmup_epochs: 3 (for lr_scheduler)
- warmup_epochs: 13 (for margin_scheduler)
- warmup_start_factor: 0.0
- gamma: 0.913
- start_epoch: 7
- patience: 15
- save_top_k: 10
  </verify>
  <done>
sv_wespeaker.yaml contains all hyperparameter updates from the research document, plus save_top_k: 10 for checkpoint averaging. Config is valid YAML that Hydra can parse.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Updated sv_wespeaker.yaml with WeSpeaker-proportional hyperparameters:
- 50 epochs (was 15)
- LR warmup from 0.0 for 3 epochs, then gamma=0.913 decay to ~0.0001 final
- Margin schedule: start at epoch 7, warmup over 13 epochs to 0.2
- Early stopping patience increased to 15
- ModelCheckpoint saves top 10 checkpoints (for averaging in Plan 02)
  </what-built>
  <how-to-verify>
1. Run training on CNCeleb with the fixed config:
   ```bash
   python src/train.py experiment=sv/sv_wespeaker datamodule=datasets/cnceleb
   ```

2. Training should run for up to 50 epochs. Monitor:
   - LR should start near 0, reach 0.1 by epoch 3, then decay
   - Margin should stay 0.0 until epoch 7, then ramp to 0.2 by epoch 20
   - Validation loss should continue decreasing (not plateau early)
   - Checkpoints directory should accumulate up to 10 checkpoint files

3. After training completes, record:
   - Final EER on CNCeleb eval (target: EER <= 8.5%)
   - Checkpoints saved in `logs/train/runs/.../checkpoints/`
   - Note: Plan 02 will average these checkpoints for potential improvement

4. If EER > 9.0%, note the actual value and training curves for diagnosis.
  </how-to-verify>
  <resume-signal>
Report one of:
- "approved: EER=X.X%, checkpoints=N" - target met, proceed to Plan 02 for checkpoint averaging
- "acceptable: EER=X.X%, checkpoints=N, notes: [observations]" - close enough, proceed to Plan 02
- "failed: EER=X.X%, diagnosis: [what went wrong]" - need iteration
  </resume-signal>
</task>

</tasks>

<verification>
Phase verification (after human checkpoint):
1. EER <= 8.5% on CNCeleb eval set (requirement BASE-01) - or acceptable with checkpoint averaging
2. Config has correct hyperparameters (requirement BASE-02)
3. Single command trains model: `python src/train.py experiment=sv/sv_wespeaker datamodule=datasets/cnceleb` (requirement BASE-03)
4. Multiple checkpoints saved for averaging workflow
</verification>

<success_criteria>
- [ ] sv_wespeaker.yaml updated with all 4 hyperparameter fixes
- [ ] save_top_k: 10 configured for checkpoint averaging
- [ ] Config is valid (Hydra can load it)
- [ ] Training completes successfully (user verification)
- [ ] Multiple checkpoints available in checkpoints directory
- [ ] Final EER <= 9.0% on CNCeleb eval (user verification) - Plan 02 may improve this
</success_criteria>

<output>
After completion, create `.planning/phases/01-cnceleb-baseline/01-01-SUMMARY.md`
</output>
